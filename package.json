{
  "name": "nfdiv-case-api",
  "version": "1.0.0",
  "description": "Utility to help creating CCD config definitions",
  "main": "index.js",
  "repository": "git@github.com:hmcts/nfdiv-case-api.git",
  "license": "MIT",
  "private": true,
  "engines": {
    "node": ">=12.0.0"
  },
  "scripts": {
    "reset-ccd-submodule": "git submodule update --init --recursive && git submodule foreach --recursive git pull origin master && yarn --cwd ccd-definition-processor install",
    "setup": "git submodule update --init --recursive && yarn --cwd ccd-definition-processor install",
    "generate-json": "yarn --cwd ccd-definition-processor xlsx2json -D ../definitions/divorce/json -i ../definitions/divorce/xlsx/ccd-config-base.xlsx && pretty-quick --pattern 'definitions/**/json/**.json'",
    "generate-excel-dev": "yarn --cwd ccd-definition-processor json2xlsx -D ../ccd-definitions/development -o ../ccd-definitions/xlsx/ccd-config-dev.xlsx",
    "generate-excel-aat": "yarn --cwd ccd-definition-processor json2xlsx -D ../ccd-definitions/aat -o ../ccd-definitions/xlsx/ccd-config-aat.xlsx",
    "generate-excel-all": "yarn generate-excel-dev && yarn generate-excel-aat"
  }
}
