server:
  port: 4013

management:
  endpoint:
    health:
      show-details: "always"
  endpoints:
    web:
      base-path: /
      exposure:
        include: health, info, prometheus

spring:
  application:
    name: NFDIV Case API
  config:
    import: "optional:configtree:/mnt/secrets/"

azure:
  application-insights:
    instrumentation-key: ${nfdiv.AppInsightsInstrumentationKey:00000000-0000-0000-0000-000000000000}

uk:
  gov:
    notify:
      api:
        key: ${nfdiv.uk-gov-notify-api-key:dummy}
        baseUrl: https://api.notifications.service.gov.uk
      email:
        templateVars:
          SAVE_SIGN_OUT:
            signInUrl: ${NOTIFY_TEMPLATE_SIGN_IN_URL:https://nfdiv.aat.platform.hmcts.net/}
            divCourtEmail: contactdivorce@justice.gov.uk
            civilPartnershipCourtEmail: endcivilpartnership@justice.gov.uk
        templates:
          english:
            SAVE_SIGN_OUT: '70dd0a1e-047f-4baa-993a-e722db17d8ac'
          welsh:
            SAVE_SIGN_OUT: '70dd0a1e-047f-4baa-993a-e722db17d8ac'

s2s-authorised:
  services: ${S2S_AUTHORISED_SERVICES:ccd_data}

idam:
  s2s-auth:
    url: ${S2S_URL:http://rpe-service-auth-provider-aat.service.core-compute-aat.internal}
