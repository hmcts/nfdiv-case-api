name: CI

on:
  pull_request:
    branches:
      - master
  push:
    branches:
      - master

jobs:
  gradle:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        task: ['check', 'contract', 'integration']
      fail-fast: false
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-java@v5
        with:
          distribution: corretto
          java-version: 21
          cache: 'gradle'
      - name: Run task
        run: ./gradlew ${{ matrix.task }}
